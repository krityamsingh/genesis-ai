# =============================================================
# GENESIS — Full requirements.txt (Vercel-compatible)
# =============================================================
# All packages used anywhere in the codebase.
# Heavy optional deps (whisper, chromadb, celery, pygame) are
# commented out — they exceed Vercel's 250MB bundle limit.
# Uncomment them only for local / Docker / Railway deployments.
# =============================================================


# ── Web Framework (REQUIRED) ──────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
starlette>=0.36.0

# Vercel serverless adapter — wraps FastAPI as a Lambda handler
mangum>=0.17.0


# ── Data Validation (REQUIRED) ────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0


# ── Database (REQUIRED) ───────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0

# PostgreSQL driver — uncomment if using DATABASE_URL=postgresql://...
# psycopg2-binary>=2.9.0


# ── AI / HuggingFace (REQUIRED — the Gemma engine) ───────────
huggingface-hub>=0.22.0


# ── Security (REQUIRED) ───────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4


# ── HTTP / Ingestion (REQUIRED) ───────────────────────────────
requests>=2.31.0
httpx>=0.27.0

# PDF ingestion (used by modules/m1_self_learner/ingestion.py)
PyPDF2>=3.0.0


# ── Config / Utils (REQUIRED) ─────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.0


# ── TTS — Text to Speech (optional, used by core/voice_interface.py) ─
# gTTS requires an internet connection (Google TTS API)
# gtts>=2.5.0

# pyttsx3 — offline TTS fallback
# pyttsx3>=2.90

# pygame — audio playback for speak_and_play()
# pygame>=2.5.0


# ── STT — Speech to Text (optional, used by ingestion.py) ─────
# openai-whisper requires ~1GB+ model download + ffmpeg system package
# Not compatible with Vercel. Use on local / Docker / Railway only.
# openai-whisper>=20231117


# ── Vector Store / Semantic Search (optional) ─────────────────
# chromadb enables full semantic search in KnowledgeGraph.
# Without it, the built-in TF-IDF fallback is used automatically.
# chromadb>=0.4.0 requires ~200MB+ and is NOT compatible with Vercel.
# Uncomment for local / Docker deployments only.
# chromadb>=0.4.0


# ── Task Queue (optional, NOT supported on Vercel) ────────────
# Celery + Redis are used by tasks/ for background jobs.
# On Vercel, serverless functions are stateless — use Railway/Render
# for a persistent Celery worker instead.
# celery>=5.3.0
# redis>=5.0.0
