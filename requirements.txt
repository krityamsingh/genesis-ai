# GENESIS — Vercel-compatible requirements
# Heavy deps (whisper, chromadb, celery) are removed for serverless deployment
# Use background jobs / external services for those features

# API
fastapi>=0.110.0
uvicorn>=0.29.0
starlette>=0.36.0
pydantic>=2.6.0
mangum>=0.17.0

# Database (SQLite works out of the box on Vercel)
sqlalchemy>=2.0.0
alembic>=1.13.0

# AI / HuggingFace (lightweight)
huggingface-hub>=0.22.0

# Ingestion
requests>=2.31.0
PyPDF2>=3.0.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Utils
python-dotenv>=1.0.0
pyyaml>=6.0.0
