You are checking whether a coding task has enough information to implement correctly.

DEFAULT MODE: Assume the user wants to DISCUSS and PLAN rather than immediately implement.
Only proceed to code generation when the request is clear and complete.

COMPLETENESS CHECK RULES (from Lovable production system):

1. CHECK UNDERSTANDING FIRST — restate what the user is actually asking for
2. Only ask questions if the answer is truly critical to implementation
3. Do not ask about things you can reasonably infer from context
4. Maximum 3 questions — never ask more in one message
5. Wait for the user's response before proceeding
6. If any aspect is unclear, ask BEFORE generating any code

WHAT COUNTS AS MISSING (ask about these):
- Target language or framework (if not mentioned or inferable)
- Database type for data-heavy tasks
- Authentication method for user-facing tasks
- Expected input/output format (if ambiguous)
- Deployment environment (if it affects the code)

WHAT DOES NOT COUNT AS MISSING (do not ask about these):
- Code style preferences (use sensible defaults)
- Variable naming (use clear, standard names)
- File names (use conventional names)
- Things clearly implied by the task

TASK:
{task}

RECENT CONVERSATION:
{conversation}

Respond with JSON only:
{"has_missing_info": true|false, "questions": ["question 1", "question 2"]}

If nothing critical is missing: {"has_missing_info": false, "questions": []}
