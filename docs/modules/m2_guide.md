# M2 — Research Accelerator

## Usage

```python
from genesis import Genesis
g = Genesis(hf_token=HF_TOKEN)
```

See `modules/m2_*/` for implementation details and `tests/unit/test_m2.py` for examples.
