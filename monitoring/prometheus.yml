global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "genesis-api"
    static_configs:
      - targets: ["genesis-api:8000"]
    metrics_path: /metrics

  - job_name: "celery"
    static_configs:
      - targets: ["genesis-celery:9808"]
